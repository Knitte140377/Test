#include <stdio.h>

int main()
{
    unsigned int countUpCounter =0;
    while(countUpCounter<=100) {
        printf("%d\n" , countUpCounter);
        countUpCounter++;
    }
    printf("Liftof\n");
    
    return 0;
 }
